pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'This is just a test build'
                echo 'Nothing is actual happening'
                echo 'It has been a ride bro'
                echo 'Build Successful'
            }
        }
    }
}
